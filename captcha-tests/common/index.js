/**
 * Common utilities and interfaces for CAPTCHA tests
 * 
 * This module exports:
 * - TestInterface: Standard interface all test modules must implement
 */

const TestInterface = require('./test-interface');

module.exports = {
  TestInterface,
  
  // Export additional utilities here as they are added
  // utils: require('./utils'),
};